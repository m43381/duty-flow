[project]
name = "duty-flow"
version = "0.1.0"
description = "Duty scheduling and optimization system"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "services/optimization",
]
